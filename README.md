# Win Theme Shift

Simple app for handling if user changes their theme from light mode to dark and vice versa

## Build

### Requirements

MSVC (Build tools for Visual Studio or Visual Studio itself)

### Executable compilation

Run `cl.exe /EHsc main.cpp /Fe:win-theme-shift.exe /link advapi32.lib`
 